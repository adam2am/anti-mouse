Okay, let's break down the user feedback and map out a plan.

**User Feedback Summary:**

1.  **Post-Spacebar Deactivation:** The grid fails to reactivate visually after using the `Space` key (which performs a click and cleanup), even though activation seems partially triggered (tooltip shows).
2.  **Subgrid Activation:** The subgrid should activate automatically whenever the cursor enters *any* main grid cell (like the highlight), not just on initial activation or after a two-key selection.

**Analysis of Current Logic & Issues:**

1.  **Post-Spacebar Issue:**
    *   **Current Logic:** `Space::` hotkey (`hotkeys.ahk`) stops tracking, sets state to `IDLE`, hides GUIs, performs `Click`, then calls `Cleanup()`. `Cleanup()` (`activation.ahk`) stops tracking again, transitions to `IDLE` (hiding GUIs), resets flags and state variables, *destroys* GUI objects (`highlight.Destroy()`, `subGrid.Destroy()`, `overlay.Destroy()`), clears the global variables holding these objects (`highlight := ""`, etc.), and calls `ForceCloseAllGuis()`. Subsequent activation via `CapsLock_Q()` *recreates* all these GUI objects.
    *   **Problem:** The failure suggests that either the `Cleanup` process isn't fully completing (leaving orphaned windows/handles despite `Destroy` and `ForceCloseAllGuis`), or the *re-creation* process in `CapsLock_Q` is failing silently (perhaps due to lingering state or an error during GUI creation/showing). The tooltip appearing indicates `CapsLock_Q` starts, but visual elements fail later.

2.  **Subgrid Activation Issue:**
    *   **Current Logic:** Subgrid activation (`TransitionToState(State_SUBGRID_STANDARD)`) occurs only in two places:
        *   `CapsLock_Q()`: If the cursor is *already* inside a cell when the grid is first activated.
        *   `HandleSecondKey()`: After a valid two-key (Col+Row or Row+Col) sequence is entered.
    *   **Hover Activation Logic:** The code for activating the subgrid when the cursor *hovers* into a cell (`TrackCursor()` in `tracking.ahk`) is explicitly **commented out** (`--- REMOVED HOVER ACTIVATION ---`). This was likely done previously to fix race conditions (Task 5.10) where hovering and pressing keys simultaneously caused issues.
    *   **Problem:** The user *wants* the hover activation behavior back, but it needs to be implemented robustly to avoid the previous conflicts.

**Needed Areas of Work:**

1.  **Cleanup & Activation Robustness:** Ensure the cleanup process is bulletproof and that subsequent activations reliably recreate and display the necessary GUI elements.
2.  **Hover-Based Subgrid Activation:** Re-implement the logic for the subgrid to appear automatically upon hovering over a main grid cell, while preventing conflicts with key presses.

**Suggested Robust Approach & User Experience:**

1.  **Cleanup/Activation:**
    *   **Refined Cleanup:** Instead of relying heavily on `ForceCloseAllGuis`, focus on *explicit and ordered* destruction within `Cleanup()` and `DeactivateGrid()`.
        1.  Stop Timers (`TrackCursor`).
        2.  Transition State (`TransitionToState(State_IDLE)`) - This should handle initial hiding.
        3.  Reset flags (`inHoldMode`, `gridActivationInProgress`).
        4.  Reset `StateMap` data (keys, indices, ultra-fast state).
        5.  **Crucially:** Iterate through `StateMap['overlays']`, `highlight`, `subGrid`. Check `IsObject()` *before* calling `.Destroy()` on each. Wrap destruction in `try/catch`.
        6.  **Crucially:** *After* destroying, explicitly set the global variables to empty strings (`highlight := ""`, `subGrid := ""`, `StateMap['overlays'] := []`, `StateMap['currentOverlay'] := ""`). This breaks the references cleanly.
    *   **Activation Verification:** In `CapsLock_Q`, after creating GUI objects (`highlight`, `subGrid`, `overlays`), add checks (`if (!IsObject(highlight))` etc.) and log/notify if creation failed *before* attempting to show or transition state.

2.  **Hover Activation:**
    *   **Re-enable Logic:** Uncomment the hover activation block in `TrackCursor()`.
    *   **Prevent Race Conditions:**
        *   **Keypress Debounce:** Introduce a state variable like `StateMap['lastKeypressTime']`. In `ProcessKeyPress` (or more specifically, when `HandleKey` is called for a *grid* key), set `StateMap['lastKeypressTime'] := A_TickCount`. In `TrackCursor`, before triggering the hover activation (`TransitionToState(State_SUBGRID_STANDARD)`), add a check: `if (A_TickCount - StateMap.Get('lastKeypressTime', 0) < 150) { highlight.Update(...); return; /* Don't transition yet */ }`. This ensures that if a grid key was pressed very recently, hover activation waits, letting the keypress logic take precedence.
        *   **First Key Check:** Retain the existing check `if (StateMap['firstKey'] == "")` within the hover logic. This correctly prevents hover activation if the user has already started a two-key selection sequence.
    *   **UX:** This provides the desired immediate feedback – the subgrid appears as soon as the user hovers over a cell (unless they *just* pressed a key), making navigation feel more fluid and intuitive, matching the highlight's behavior. The debounce prevents accidental activation during typing.

---

## Macro Plan for Changes (Stages)

Here's a staged plan focusing on robustness and user experience:

**Stage 1: Stabilize Cleanup and Activation (Address User Problem #1)**

*   **Goal:** Ensure 100% reliable grid activation after any cleanup sequence (`Space`, `Escape`, clicks, etc.).
*   **Tasks:**
    1.  **Refactor `Cleanup()` / `DeactivateGrid()`:**
        *   Implement the "Refined Cleanup" steps described above: ordered destruction, `IsObject` checks, explicit variable clearing (`:= ""`), `try/catch` around `Destroy()`.
        *   Minimize or remove the use of `ForceCloseAllGuis()`, relying on the explicit loop and `Destroy()`.
        *   Ensure `TrackCursor` timer is reliably stopped (`SetTimer(TrackCursor, 0)`).
        *   Add detailed logging within these functions to trace object destruction and variable resets.
    2.  **Refactor `CapsLock_Q()` (Activation):**
        *   Add explicit `IsObject()` checks *after* creating `highlight`, `subGrid`, and `overlays`. Log errors and potentially `Cleanup()` again if creation fails.
    3.  **Testing:** Rigorously test activation cycles: activate -> space -> activate -> escape -> activate -> click -> activate. Verify grid appears correctly every time. Check logs for errors during cleanup or creation.

**Stage 2: Implement Robust Hover Activation (Address User Problem #2)**

*   **Goal:** Subgrid activates automatically and reliably upon hovering into a cell when the main grid is visible, without conflicting with key presses.
*   **Tasks:**
    1.  **Modify `tracking.ahk` (`TrackCursor()`):**
        *   Uncomment the hover activation logic block (the one that checks `currentCellKey != lastTrackedCellKey_GridVisible`, gets boundaries, etc.).
        *   **Implement Debounce:** Add the `StateMap['lastKeypressTime']` check as described above before calling `TransitionToState(State_SUBGRID_STANDARD)`. If debouncing, *only* update the highlight.
        *   **Retain `firstKey` Check:** Ensure the `if (StateMap['firstKey'] == "")` check remains within the hover logic.
        *   Ensure the logic for leaving a cell (`StartNewSelection("")`) is still functional for cells activated via hover.
    2.  **Modify `core/key_processing.ahk` / `core/grid_keys.ahk`:**
        *   Identify the best place to record the keypress time (likely start of `HandleKey` when `currentState == State_GRID_VISIBLE`). Add `StateMap['lastKeypressTime'] := A_TickCount`.
    3.  **Testing:**
        *   Verify subgrid appears instantly on hover when grid is idle (`firstKey` is empty).
        *   Verify subgrid does *not* appear if hovering while having already pressed the first key (`firstKey` is set).
        *   Verify subgrid activation is delayed or skipped if hovering occurs immediately after pressing a grid key (test the debounce).
        *   Verify moving the cursor out of a hover-activated cell correctly transitions back to `GRID_VISIBLE`.

**Stage 3: Refinement and Polish**

*   **Goal:** Smooth out transitions, review state logic, optimize, and ensure overall best practices.
*   **Tasks:**
    1.  **Review State Transitions:** Ensure `TransitionToState` handles all necessary GUI showing/hiding cleanly for all state changes, especially with the new hover activation.
    2.  **Ultra-Fast Mode Check:** Re-evaluate the `#HotIf` condition for the row key `up` events in `hotkeys.ahk`. Given `HandleRowKeyRelease` now transitions state explicitly, the condition `#HotIf currentState == State_SUBGRID_STANDARD && StateMap.Get("inUltraFastMode", false)` seems incorrect. It should likely be `#HotIf currentState == State_SUBGRID_ULTRAFAST`. Test this change carefully.
    3.  **Code Consolidation:** Double-check that `g_firstKeyPressed` is fully removed and replaced by `StateMap['firstKey']`.
    4.  **Performance:** Consider if the `TrackCursor` interval (50ms) needs adjustment.
    5.  **Configuration:** Add a boolean setting in `config.ahk` and `settings_gui.ahk` to enable/disable the new hover activation feature (`enableHoverActivation := true`). Add the debounce time (`hoverActivationDebounce := 150`) as a configurable value.
    6.  **Documentation/Comments:** Update comments to reflect the new hover logic and cleanup process. Remove obsolete comments.
    7.  **Final Testing:** Perform comprehensive testing of all features and edge cases.







# Current Micro-Layout-Todos (step-by-step)

## User Feedback before the task start:
1 - after space cant call for grid again, just not appearing
2 - it doesnt recognize proper smooth movement between cells
activate grid > Im on RK cell > press O > it should go RO (because it RK > RO) instead first press it jumps to QO
same with collumns 
RK > Press D > should go DK, it goes DP, not snapping to the cell where my cursor is right now 


Okay, let's refine the task list based on the current state and the user feedback, focusing on a robust and scalable approach with best practices.

The core issues to address are:

1.  **Stability/Cleanup:** The grid fails to reactivate after `Space` cleanup, indicating issues with GUI object destruction or recreation. Crashes during rapid key presses (A->S->P) point to race conditions or invalid object access.
2.  **Hover Activation:** The user wants the subgrid to activate immediately upon hovering over a main grid cell, which is currently disabled.

We will prioritize stability first, then implement the desired hover behavior robustly.

---

## AntiMouse Modular - Refined Micro-Plan (Based on User Feedback & Current State)

**Core Goals:**

1.  Achieve rock-solid stability during activation, deactivation, and rapid interactions.
2.  Implement intuitive, immediate hover-based subgrid activation without conflicts.


## User Feedback after (1.1 done):
- this is great that now I can press space and its working nicely now
but for some reason now my even grid navigation broke, is it okay till while w do 1s phase? 


 
|ID    | To-Do (Actionable) + Desired Behavior                                                                                                                                                                                                                         | Status | Priority | Files Affected                                                                                             | Approach/Variables + Confidence (1-10)                                                                                                                                                                                                                                                                                                                                                        | Method to Test/Verify                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | How is it different?                                                                                                                                                                                                | Notes+Conclusion in a process                                                                                                                                                                                                                                                        | Dependencies |
| :---- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----- | :------- | :--------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------- |
|       | **Stage 1: Achieve Core Stability & Robust Cleanup**                                                                                                                                                                                                           |        |          |                                                                                                            | **Focus:** Eliminate crashes and ensure reliable grid show/hide cycles, especially after cleanup.                                                                                                                                                                                                                                                                                                 |                                                                                                                                                                                                                                                                                            |                                                                                                                                                                                                                             | Addresses User Problem #1. Prerequisite for all other UX improvements.                                                                                                                                                         |              |
| S1.1  | **Implement Robust GUI Object Handling:** Before *every* call to GUI methods (`.Update`, `.Hide`, `.Show`, `.Destroy`, `.Move`, etc.) on `highlight`, `subGrid`, or `overlay` objects, add `if (IsObject(guiObject))` checks. Wrap calls within `try/catch` blocks. Log warnings on failure. | `✅`   | **P1 🔴** | `activation.ahk`, `core/grid_keys.ahk`, `core/subgrid_keys.ahk`, `core/tracking.ahk`, `core/state_transitions.ahk`, `hotkeys.ahk` | **Approach:** Systematically review all GUI interactions. Use `IsObject()` guard. Use `try/catch { LogToFile(...) }`. **Variables:** `highlight`, `subGrid`, `StateMap['overlays']`, `StateMap['currentOverlay']`. **Confidence:** 9/10 (Standard practice, requires thoroughness)                                                                                                           | Trigger rapid key presses (A->S->P), rapid mouse movements, activate/deactivate sequences (Space, Esc, Click). Monitor logs for warnings instead of script-crashing errors. Verify script remains responsive.                                                                                 | **Difference:** Proactively prevents errors from accessing potentially destroyed/invalid GUI handles during rapid state changes or cleanup, replacing crashes with logged warnings.                                        | Foundation for stability. Addresses Tasks 5.7, 5.10 from the old list.                                                                                                                                        | None         |
| S1.2  | **Refine Cleanup Logic (`Cleanup` / `DeactivateGrid`):** Ensure ordered, explicit cleanup: Stop timers -> Transition State -> Reset Flags/StateMap -> Destroy GUIs (using S1.1 checks) -> Clear Global Vars (`:= ""`). Minimize/remove `ForceCloseAllGuis`. Add detailed logging. | `[]`   | **P1 🔴** | `activation.ahk`, `utils.ahk`?                                                                             | **Approach:** Rework `Cleanup` and `DeactivateGrid` following the explicit steps. Use loops with S1.1 checks for destroying overlays. Explicitly set `highlight := ""`, `subGrid := ""`, `StateMap['overlays'] := []`. **Variables:** `highlight`, `subGrid`, `StateMap`, `g_ModifierState`, `gridActivationInProgress`. **Confidence:** 8/10 (Logical flow, needs careful implementation) | Activate -> Space -> Activate. Activate -> Esc -> Activate. Activate -> Click Overlay -> Activate. Verify grid consistently reappears visually each time. Check logs for clean destruction and variable clearing. Verify no lingering GUI windows (`ahk_class AutoHotkeyGUI`). | **Difference:** More reliable and traceable cleanup compared to potentially brute-force `ForceCloseAllGuis`. Prevents state/object leakage that likely causes the post-Spacebar issue. Addresses Tasks 2.14, 5.6, 6.10. | Directly tackles User Problem #1.                                                                                                                                                                             | S1.1         |
| S1.3  | **Refine Activation Logic (`CapsLock_Q`):** After creating GUIs (`highlight`, `subGrid`, `overlays`), add `IsObject()` checks. If any creation failed, log error, call `Cleanup()`, and `return` early to prevent proceeding with invalid objects.                 | `[]`   | **P1 🔴** | `activation.ahk`                                                                                           | **Approach:** Add checks post-object creation (`new HighlightOverlay()`, etc.). `if (!IsObject(highlight) || !IsObject(subGrid)) { ... Cleanup(); return }`. **Variables:** `highlight`, `subGrid`, `StateMap['overlays']`. **Confidence:** 9/10 (Simple validation)                                                                                                                | Intentionally break GUI creation (e.g., syntax error in class) and verify activation fails gracefully with logs/cleanup, rather than errors later. Test normal activation continues to work.                                | **Difference:** Prevents activation from proceeding if essential GUI components failed to initialize, avoiding later errors during transitions or key presses.                                                    | Ensures `CapsLock_Q` doesn't contribute to instability.                                                                                                                                                               | S1.1, S1.2   |
| S1.4  | **Stabilize Key Handling (`HandleKey`, `HandleFirstKey`, `HandleSecondKey`):** Ensure S1.1 checks are applied. Specifically review `HandleSecondKey`'s interaction with `StartNewSelection` on invalid sequences (Task 5.10 fix) - ensure state is consistent before calling `StartNewSelection`. | `[]`   | **P1 🔴** | `core/grid_keys.ahk`, `core/state_transitions.ahk`                                                         | **Approach:** Verify S1.1 checks. Review logic flow for Col->Col / Row->Row: ensure `StateMap['firstKey']` is cleared *before* `StartNewSelection` is called. Add logging around this transition. **Variables:** `StateMap`, `highlight`, `subGrid`. **Confidence:** 8/10 (Focuses on a known complex interaction point)                                                                       | Test Col->Col and Row->Row sequences rapidly (e.g., Q->W, A->S). Verify script doesn't crash and logs show clean transition via `StartNewSelection`. Test A->S->P sequence again.                                       | **Difference:** Addresses the specific A->S->P crash scenario by ensuring state is managed correctly during invalid sequence handling.                                                                        | Crucial for fixing reported crashes (Task 5.7).                                                                                                                                                             | S1.1         |
|       | **Stage 2: Implement Robust Hover Activation**                                                                                                                                                                                                                |        |          |                                                                                                            | **Focus:** Implement the desired UX where subgrid appears on hover, preventing conflicts.                                                                                                                                                                                                                                                                                                         |                                                                                                                                                                                                                                                                                            |                                                                                                                                                                                                                             | Addresses User Problem #2. Builds upon stable foundation from Stage 1.                                                                                                                                        |              |
| S2.1  | **Re-enable Hover Logic in `TrackCursor`:** Uncomment the block in `TrackCursor` (`GRID_VISIBLE` state) that finds `currentCellKey`, gets boundaries, and *would* activate subgrid. (Do not uncomment the `TransitionToState` call yet).                       | `[]`   | **P2 🔵** | `core/tracking.ahk`                                                                                        | **Approach:** Locate `--- REMOVED HOVER ACTIVATION ---` block. Uncomment boundary checks and highlight update logic. Keep `TransitionToState` commented for now. Apply S1.1 checks. **Variables:** `highlight`, `currentCellKey`, `lastTrackedCellKey_GridVisible`. **Confidence:** 9/10 (Simple uncommenting, safety checks)                                                                   | Activate grid, move cursor. Verify highlight follows cursor correctly. Verify moving off cells hides highlight. Verify logs show cell changes.                                                                        | **Difference:** Restores the necessary checks and highlight updates required for hover activation, preparing for the state transition.                                                                         | Preparation step. Task 6.15 partially addressed.                                                                                                                                                            | S1.1 - S1.4  |
| S2.2  | **Implement Hover Activation Transition:** In `TrackCursor` (after S2.1), uncomment/add the logic: `Set StateMap['activeCellKey'] = currentCellKey`, `subGrid.Update(...)`, `TransitionToState(State_SUBGRID_STANDARD)`. Remove the old `if (StateMap['firstKey'] == "")` check if present. | `[]`   | **P2 🔴** | `core/tracking.ahk`, `core/state_transitions.ahk`                                                          | **Approach:** Inside the block from S2.1, after `highlight.Update()`, add the state setting, `subGrid.Update()` (with S1.1 checks), and `TransitionToState()` call. **Variables:** `StateMap['activeCellKey']`, `subGrid`, `currentState`. **Confidence:** 8/10 (Core logic, potential for timing issues addressed next)                                                              | Activate grid, move cursor into a cell. Verify subgrid appears instantly. Move to another cell, verify subgrid follows. Verify logs show transition to `SUBGRID_STANDARD` on hover.                                | **Difference:** Enables the core hover-activation behavior requested by the user. Addresses Tasks 2.17, 6.5, 6.15.                                                                                             | The primary UX change.                                                                                                                                                                                          | S2.1         |
| S2.3  | **Implement Keypress Precedence over Hover:** Add `StateMap['lastKeypressTime']`. Set it in `HandleKey` (when state is `GRID_VISIBLE`). In `TrackCursor`, *before* the `TransitionToState` call (S2.2), add `if (A_TickCount - StateMap.Get('lastKeypressTime', 0) < 150) return;`. | `[]`   | **P2 🔴** | `core/tracking.ahk`, `core/grid_keys.ahk`                                                                  | **Approach:** Define new `StateMap` key. Add `StateMap['lastKeypressTime'] := A_TickCount` at start of `HandleKey`. Add debounce check (`if A_TickCount...`) in `TrackCursor`. **Variables:** `StateMap['lastKeypressTime']`. **Confidence:** 8/10 (Standard debounce logic, needs careful placement)                                                                                    | Activate grid. Press a grid key ('F'), immediately move cursor to another cell ('L'). Verify subgrid does *not* appear instantly for 'L' (due to debounce), but 'F' is processed correctly. Test varying delays.         | **Difference:** Prevents race condition where hover activates subgrid *while* a keypress is being processed, ensuring keypresses take priority. Addresses Task 6.6.                                             | Critical robustness fix for hover activation.                                                                                                                                                               | S2.2         |
| S2.4  | **Ensure Hover Exit Works:** Verify `TrackCursor` logic for `SUBGRID_STANDARD`/`ULTRAFAST` states correctly calls `StartNewSelection("")` when cursor leaves the `activeCellKey` boundaries (set via hover). Apply S1.1 checks to boundary getting.               | `[]`   | **P2 🔵** | `core/tracking.ahk`, `core/state_transitions.ahk`                                                          | **Approach:** Review the existing block checking `cursorInsideCell`. Ensure `GetCellBoundaries` is wrapped with S1.1 checks. Verify `StartNewSelection("")` is called correctly. **Variables:** `StateMap['activeCellKey']`, `currentState`. **Confidence:** 9/10 (Verifying existing logic with added checks)                                                                                | Activate hover-subgrid by moving into cell. Move cursor outside the cell boundaries. Verify subgrid disappears and state returns to `GRID_VISIBLE` (highlight should reappear if hovering another cell). Check logs. | **Difference:** Ensures that leaving a cell activated by hover correctly reverts the state, maintaining consistency. Addresses Task 6.7.                                                                         | Ensures hover feature is fully functional (entry and exit).                                                                                                                                                   | S2.2         |
|       | **Stage 3: Refinements & Configuration**                                                                                                                                                                                                                      |        |          |                                                                                                            | **Focus:** Polish UX, fix minor inconsistencies, add configuration options.                                                                                                                                                                                                                                                                                                               |                                                                                                                                                                                                                                                                                            |                                                                                                                                                                                                                             | Improves overall quality and user control.                                                                                                                                                                    |              |
| S3.1  | **Fix Ultra-Fast Key Up Hotkey:** Change `#HotIf` for row key `up` events in `hotkeys.ahk` from `currentState == State_SUBGRID_STANDARD && StateMap.Get("inUltraFastMode", false)` to `currentState == State_SUBGRID_ULTRAFAST`.                          | `[]`   | **P3 🔵** | `hotkeys.ahk`                                                                                              | **Approach:** Edit the `#HotIf` directive directly. **Variables:** `currentState`. **Confidence:** 9/10 (Corrects a clear logical contradiction based on Task 6.8)                                                                                                                                                                                                                            | Enter ultra-fast mode (select cell, hold row key). Release the row key. Verify `HandleRowKeyRelease` is called (check logs) and state transitions back to `SUBGRID_STANDARD`.                                     | **Difference:** Fixes the condition under which the ultra-fast mode exit trigger fires, making it consistent with the state machine logic. Addresses Task 6.14.                                            | Corrects known bug.                                                                                                                                                                                           | Stage 1, Task 6.8 (Implies Ultra-Fast entry works minimally) |
| S3.2  | **Add Hover Activation Config:** Add `enableHoverActivation` flag to `config.ahk`, `settings_gui.ahk` (checkbox), `memory_settings.ahk` (save/load). Wrap the `TransitionToState` call in `TrackCursor` (S2.2) with `if (enableHoverActivation) { ... }`.        | `[]`   | **P3 🟢** | `config.ahk`, `settings_gui.ahk`, `memory_settings.ahk`, `core/tracking.ahk`                                  | **Approach:** Add global var, GUI control, IniRead/Write. Add `if (enableHoverActivation)` guard in `TrackCursor`. **Variables:** `enableHoverActivation`. **Confidence:** 9/10 (Standard config implementation)                                                                                                                                                                             | Toggle setting in GUI & save. Restart script (or reload settings if implemented). Verify hover activation behavior matches the setting.                                                                       | **Difference:** Allows users to disable hover activation if they prefer the two-key selection method only. Addresses Task 4.5 partially.                                                                      | Provides user choice.                                                                                                                                                                                         | Stage 2      |
| S3.3  | **Add Cursor Move Config:** Add `moveCursorOnSelection` flag to `config.ahk`, `settings_gui.ahk`, `memory_settings.ahk`. Wrap the `MouseMove` call in `HandleSecondKey` (Task 6.13) with `if (moveCursorOnSelection) { ... }`.                                 | `[]`   | **P3 🟢** | `config.ahk`, `settings_gui.ahk`, `memory_settings.ahk`, `core/grid_keys.ahk`                                  | **Approach:** Add global var, GUI control, IniRead/Write. Add `if (moveCursorOnSelection)` guard in `HandleSecondKey`. **Variables:** `moveCursorOnSelection`. **Confidence:** 9/10 (Standard config implementation)                                                                                                                                                                           | Toggle setting in GUI & save. Restart script. Select cells using two keys. Verify cursor movement behavior matches the setting.                                                                             | **Difference:** Makes the cursor jump on second key press optional, based on user preference. Addresses Task 6.16.                                                                                              | Provides user choice.                                                                                                                                                                                         | Task 6.13 (Implies fix is working) |
| S3.4  | **Final Code Cleanup & Review:** Remove obsolete comments/code (e.g., old hover logic if disabled, `g_firstKeyPressed` remnants). Ensure logging is informative but not excessive. Add comments for new hover/stability logic.                                | `[]`   | **P3 🟢** | All modified files                                                                                         | **Approach:** Code review focusing on clarity, removal of dead code/comments, appropriate logging levels. **Confidence:** 8/10 (Standard cleanup)                                                                                                                                                                                                                                        | Code review. Run script and check logs for clarity and relevance.                                                                                                                                               | **Difference:** Improves maintainability and readability of the final codebase. Addresses Tasks 5.2, 6.12.                                                                                                   | Good practice.                                                                                                                                                                                              | Stage 1 & 2  |
|       | **Stage 4: Ultra-Fast Mode Full Implementation (Optional)**                                                                                                                                                                                                   |        |          |                                                                                                            | **Focus:** Implement the detailed logic for entering and using the Ultra-Fast mode.                                                                                                                                                                                                                                                                                                              |                                                                                                                                                                                                                                                                                            |                                                                                                                                                                                                                             | Lower priority until core UX/stability is perfect.                                                                                                                                                            |              |
| S4.1  | **Implement Row Key Hold Detection:** Choose method (e.g., timer started in `HandleSecondKey`, checked in `TrackCursor`, or dedicated key-up/down handlers) to detect row key hold > `rowKeyHoldThreshold` after cell selection. Set `StateMap['rowKeyHeldTime']`.      | `[]`   | **P4 🟣** | `core/grid_keys.ahk`?, `core/tracking.ahk`?, `hotkeys.ahk`?                                                | **Approach:** Evaluate complexity vs reliability. Timer seems plausible. Start in `HandleSecondKey` if row key was last. Check time in `TrackCursor` or dedicated timer callback. **Variables:** `StateMap['rowKeyHeldTime']`, `StateMap['activeRowKey']`. **Confidence:** 6/10 (Requires careful design to integrate with existing states)                                             | Select cell (row key last). Hold key > threshold. Verify hold is detected (logs). Tap key < threshold. Verify hold is *not* detected.                                                                         | **Difference:** Implements the trigger mechanism for Ultra-Fast mode. Addresses Task 3.1.                                                                                                                 | Core of Ultra-Fast mode.                                                                                                                                                                                    | Stage 1 & 2  |
| S4.2  | **Implement Ultra-Fast State Transition:** If hold detected (S4.1) before key release, call `TransitionToState(State_SUBGRID_ULTRAFAST)`. Ensure `TransitionToState` handles the `SUBGRID_ULTRAFAST` entry actions (show highlight, switch+show subgrid). Cancel timer on key release. | `[]`   | **P4 🔵** | Timer callback / Hold detection logic, `core/state_transitions.ahk`, `hotkeys.ahk` (key up handler)           | **Approach:** Add `TransitionToState` call in hold detection logic. Implement `SUBGRID_ULTRAFAST` entry actions in `TransitionToState`. Add timer cancellation to key `up` handler (already partially exists). **Variables:** `currentState`, `subGrid`, `highlight`. **Confidence:** 8/10 (Builds on existing state transition logic)                                                        | Hold row key > threshold. Verify state changes to `SUBGRID_ULTRAFAST` (logs), highlight stays, subgrid switches to 3x4 layout and shows. Release key, verify transition back to `STANDARD` (Task S3.1).              | **Difference:** Connects hold detection to the state machine and visual changes. Addresses Task 3.2, 3.3.                                                                                                       | Links detection to state.                                                                                                                                                                                   | S4.1, S3.1   |
| S4.3  | **Implement Ultra-Fast Key Handling:** Implement `ProcessUltraFastKey` in `core/subgrid_keys.ahk` to map QWER... keys to coords using `subGrid.GetUltraFastTargetCoordinates()`, move mouse, update memory (with "ultra:" prefix). Ensure S1.1 checks used.         | `[]`   | **P4 🔵** | `core/subgrid_keys.ahk`                                                                                    | **Approach:** Fill in `ProcessUltraFastKey`. Use `subGrid` method. Call `MouseMove`. Call `UpdateCellMemory`. **Variables:** `StateMap['activeSubCellKey']`, `cellMemory`. **Confidence:** 8/10 (Straightforward implementation based on standard subgrid)                                                                                                                                       | Enter ultra-fast mode. While holding row key, press Q, W, E, etc. Verify mouse moves to correct sub-cell positions within the main cell highlight. Check logs for `ProcessUltraFastKey` execution.               | **Difference:** Enables actual navigation within the ultra-fast layout. Addresses Task 3.4.                                                                                                                 | Core ultra-fast functionality.                                                                                                                                                                              | S4.2         |