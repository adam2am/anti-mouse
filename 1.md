# Ultra-Fast Subgrid Mode - Feature Plan

## Overview
This feature adds a new "Ultra-Fast Subgrid" mode that allows for faster and more efficient navigation within a cell using a held row key plus left-hand keys for subgrid selection. This is implemented as a configurable setting that can be toggled in the settings menu.

## Idea Functionality
[
### Workflow, scenarios
1.  **Initial Grid Activation**:
    *   CapsLock(hold) + Key (e.g., Q/1/2) to activate the grid
    *   Classic grid is always shown first (ghvn), regardless of settings

2.  **Cell Selection(same)**:
    *   Press a column key (e.g., Q)
    *   Press a row key (e.g., L) to select cell "QL"

3.  **Subgrid Mode Options**:
    *   **Tap Row Key(default behaviour)**: Traditional GHBN subgrid appears (2x2 grid)
    *   **But Hold Row Key**: If within a (150ms) physical hold being detected, ultra-fast subgrid being shown with new QWERASDFZXCV keys as being active to navigate within it (3x4 grid). If released > GHVN grid comes back. Simple robust approach with show/hide, without refreshing (right now there is a conflict with jumping to cell if row/collumn being clicked, needs to be resolved)

4.  **Ultra-Fast Subgrid Usage**:
    *   While holding the row key (e.g., L), press any left-hand key (QWERASDFZXCV)
    *   Mouse moves to the corresponding position in the 3x4 grid

### Subgrid Layouts
-   **Traditional**: 2x2 grid with keys G/H/B/N
-   **Ultra-Fast**: 3x4 grid with keys:
    ```
    q w e r
    a s d f
    z x c v
    ```
]



## Implementation Plan & Status

1.  [] **Fix Key Handling Conflict:**
    *   **Problem:** Pressing navigation row key (UIOJKL etc.) while holding the row key for ultra-fast mode incorrectly triggered `StartNewSelection`, resetting the grid.

2.  [] **Refine Row Key Hold/Release Logic = making sure holding is robust and trackable:**
    *   **Goal:** Ensure the transition between standard (GHBN) and ultra-fast (QWER...) subgrids based on row key hold/release is smooth and reliable.

3.  [] **Add Configuration Parameters (config.ahk):**
    *   Add enableUltraFast, rowKeyHoldThreshold, and ultraFastSubGridKeys to make feature configurable.

4.  [] **Extend State Tracking (state.ahk):**
    *   Add rowKeyHeldTime, activeRowKey, and inUltraFastMode to StateMap for tracking the feature state.

5.  [] **Enhance SubGridOverlay (gui_classes.ahk):**
    *   Add methods to toggle between standard 2×2 and ultra-fast 3×4 grid visual layouts.

6.  [] **Update HandleKey (core_logic.ahk):**
    *   Add row key press time tracking to detect when completing cell selection with held row key.

7.  [] **Implement Key Release Detection (hotkeys.ahk):**
    *   Add key-up handlers for row keys to detect when user releases the held row key.

8.  [] **Create HandleRowKeyRelease (core_logic.ahk):**
    *   New function to switch back to standard mode when row key is released.

9.  [] **Update ProcessKeyPress (core_logic.ahk):**
    *   Extend to route ultra-fast keys to the correct handler when in ultra-fast mode.

10. [] **Implement Ultra-Fast Key Handler (core_logic.ahk):**
    *   New HandleUltraFastKey function to process 3×4 grid navigation and save positions to memory.

11. [] **Add Timer Check in TrackCursor (core_logic.ahk):**
    *   Detect when row key is held long enough to trigger ultra-fast mode transition.

12. [] **Add Settings GUI Option (settings_gui.ahk):**
    *   Add interface toggle to enable/disable feature and adjust hold threshold.

13. [] **Fix Conflicts in StartNewSelection (core_logic.ahk):**
    *   Prevent grid reset when pressing keys during ultra-fast mode.

14. [] **Add Visual Indication:**
    *   Add visual cue (color/label) so user knows when ultra-fast mode is active.

15. [] **Debug & Testing Mode:**
    *   Add specific debug logging for ultra-fast mode to assist with timing tuning.

!!!!!!!!!!!! IMPORTANT TO MAKE IT SCALABLE AND ROBUST STRAIGHTFORWARD TRACK STUFF, UsE BEST CODE PRACTICES. IF SMTH IS NOT FOLLOWING IT = REFINE IT. AS SIMPLE AND READABLE AS POSSIBLE YET FUNCTIONALITY ON POINT 