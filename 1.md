# Ultra-Fast Subgrid Mode - Feature Plan

## Overview
This feature adds a new "Ultra-Fast Subgrid" mode that allows for faster and more efficient navigation within a cell using a held row key plus left-hand keys for subgrid selection. This is implemented as a configurable setting that can be toggled in the settings menu.

## Idea Functionality
[
### Workflow, scenarios
1.  **Initial Grid Activation**:
    *   CapsLock(hold) + Key (e.g., Q/1/2) to activate the grid
    *   Classic grid is always shown first (ghvn), regardless of settings

2.  **Cell Selection(same)**:
    *   Press a column key (e.g., Q)
    *   Press a row key (e.g., L) to select cell "QL"

3.  **Subgrid Mode Options**:
    *   **Tap Row Key(default behaviour)**: Traditional GHBN subgrid appears (2x2 grid)
    *   **But Hold Row Key**: If within a (150ms) physical hold being detected, ultra-fast subgrid being shown with new QWERASDFZXCV keys as being active to navigate within it (3x4 grid). If released > GHVN grid comes back. Simple robust approach with show/hide, without refreshing (right now there is a conflict with jumping to cell if row/collumn being clicked, needs to be resolved)

4.  **Ultra-Fast Subgrid Usage**:
    *   While holding the row key (e.g., L), press any left-hand key (QWERASDFZXCV)
    *   Mouse moves to the corresponding position in the 3x4 grid

### Subgrid Layouts
-   **Traditional**: 2x2 grid with keys G/H/B/N
-   **Ultra-Fast**: 3x4 grid with keys:
    ```
    q w e r
    a s d f
    z x c v
    ```
]



## Implementation Plan & Status

**CURRENT FOCUS: Fixing State Management During Key Input**

1.  [✓] **Diagnose State During Key Hold (Deep Logging):**
    ; Status: Logging and `GetKeyState` added. Log analysis revealed the root cause.
    ; Goal: Understand the exact sequence of state changes (`currentState`, `firstKey`, `activeRowKey`, `inUltraFastMode`) during the Q->K(hold) scenario.
    ; Finding: The log clearly shows that `TrackCursor` is changing `currentState` from `GRID_VISIBLE` to `SUBGRID_ACTIVE` based *only* on mouse movement, even when `firstKey` is already set and waiting for the second key. This premature state change causes subsequent key presses (including the intended second key or its auto-repeats) to incorrectly trigger `StartNewSelection`, resetting the sequence.

2.  [✓] **Fix State Instability (`TrackCursor` Interference):**
    ; Status: Fixed. Added check `StateMap['firstKey'] == ""`.
    ; Goal: Prevent `TrackCursor` from changing `currentState` to `SUBGRID_ACTIVE` when a key sequence is in progress (i.e., when `StateMap['firstKey']` is not empty). The transition to `SUBGRID_ACTIVE` must *only* happen within `HandleKey` after a valid second key press.
    ; Action: Modified the `TrackCursor` logic. Added the condition `StateMap['firstKey'] == ""` to the section that sets `currentState := "SUBGRID_ACTIVE"` based on mouse movement.

3.  [!] **Fix Ultra-Fast Activation Check in `TrackCursor`:**
    ; Status: Dependent on #2 verification. The fix in #2 should make the state stable enough for this check.
    ; Goal: Ensure the hold duration check works now that the state should be stable.
    ; Action: Re-test Q->K hold scenario and verify via logging that `TrackCursor` now correctly measures `heldTime` against `rowKeyHoldThreshold` without state resets.

4.  [!] **Add Explicit Hold Detection Feedback:**
    ; Status: Implemented, keep.

5.  [✓] **Test Key Release Deactivation Robustly:**
    ; Status: Fix implemented, needs testing after #1/#2/#3 fixed.

6.  [✓] **Test State Reset (Escape/Reactivation):**
    ; Status: Fix implemented, needs testing after #1/#2/#3 fixed.

---
**LOWER PRIORITY / FOR LATER:**

7.  [!] **Investigate "invalid key ultra-r":**
    ; New issue: "invalid key ultra-r" message reported.
    ; Goal: Find why a valid ultra-fast key press fails.
    ; Action: After fixing activation (#1-#3), debug `HandleUltraFastKey` and `GetUltraFastTargetCoordinates`.

8.  [✓] **Fix Row Key Hold Logic in `HandleKey` (Refactor):**
    ; Status: Refactor implemented, but ineffective due to state instability. Logic likely correct but needs stable state from #1/#2 fix.

9.  [!] **Investigate Accidental Ultra-Fast Activation (Fast Key Presses):**
    ; Bug: Activates unintentionally with rapid Col+Row presses.
    ; Goal: Prevent accidental activation.
    ; Action: Analyze timing, potentially add safeguards/delays after fixing core logic.

10. [!] **Fix `subGrid` Object Error:**
    ; Error: "This value of type 'String'..."
    ; Status: Lower priority, monitor if it reappears.

11. [ ] **Verify State Transitions and UI Updates:**
    ; Final verification step after critical bugs are fixed.

---
**Previous Steps done 100%**
1.  [X] **Fix Subgrid Not Following Cursor:**
    ; User confirmed FIXED. Subgrid now correctly follows the cursor.

7.  [X] **Add Configuration Parameters (config.ahk):**
    ; Add enableUltraFast, rowKeyHoldThreshold, and ultraFastSubGridKeys to make feature configurable.

8.  [X] **Extend State Tracking (state.ahk):**
    ; Add rowKeyHeldTime, activeRowKey, and inUltraFastMode to StateMap for tracking the feature state.

**Previous Steps (Need Verification After Debugging):**

9.  [X] **Enhance SubGridOverlay (gui_classes.ahk):**
    ; Add methods to toggle between standard 2×2 and ultra-fast 3×4 grid visual layouts.

10. [✓] **Update HandleKey (core_logic.ahk):**
    ; Add row key press time tracking *[Logic needs fixing - see #1]*. --> Auto-repeat part fixed.

11. [✓] **Implement Key Release Detection (hotkeys.ahk):**
    ; Add key-up handlers for row keys to detect when user releases the held row key *[Fix implemented - see #2 test]*. 

12. [✓] **Create HandleRowKeyRelease (core_logic.ahk):**
    ; New function to switch back to standard mode when row key is released *[Fix implemented - see #2 test]*.

13. [✓] **Update ProcessKeyPress (core_logic.ahk):**
    ; Extend to route ultra-fast keys to the correct handler when in ultra-fast mode.

14. [✓] **Implement Ultra-Fast Key Handler (core_logic.ahk):**
    ; New HandleUltraFastKey function to process 3×4 grid navigation and save positions to memory.

15. [✓] **Add Timer Check in TrackCursor (core_logic.ahk):**
    ; Detect when row key is held long enough to trigger ultra-fast mode transition *[NEEDS FIXING - see #1]*.

16. [✓] **Add Settings GUI Option (settings_gui.ahk):**
    ; Add interface toggle to enable/disable feature and adjust hold threshold.

17. [✓] **Fix Conflicts in StartNewSelection (core_logic.ahk):**
    ; Prevent grid reset when pressing keys during ultra-fast mode.

18. [✓] **Add Visual Indication:**
    ; Add visual cue (color/label) so user knows when ultra-fast mode is active.

19. [✓] **Debug & Testing Mode:**
    ; Add specific debug logging for ultra-fast mode to assist with timing tuning *[Action: Modify HandleKey debug to log to file]*. --> Logging implemented.

!!!!!!!!!!!! IMPORTANT TO MAKE IT SCALABLE AND ROBUST STRAIGHTFORWARD TRACK STUFF, UsE BEST CODE PRACTICES. IF SMTH IS NOT FOLLOWING IT = REFINE IT. AS SIMPLE AND READABLE AS POSSIBLE YET FUNCTIONALITY ON POINT 

/ based on a implementation plan @1.md  go do @antimouse-modular  it and each time  you make it happen, something is implemented -- checkmark it in 1.md, or if need extra testing, write 

[] = didnt start,
! = attention, /broken,
✓ = need testing, 
X = done 100%


-->>>
# User feedback + scenarios:
I go to grid first time = I see default grid
I press Q I press K -> it jumps to cell. I hold K its refreshing tool tips rapid crazy
branch row (change the row) and 2nd I cant even read its changing too fast, 
I guess it doesnt understand that I'm holding? I dont know.

me holding row key does refreshes the grid and just blinking with blue border colour
its blinking with blue and nothing within it, it says row chached to K, sellect collumn

After I managed to activate a ultrafastgrid!! YAY
I made it by clicking fast to buttons Q+L at the same time super fast together
Sad: its not turning off even tho I'm not holding row key at all, its like turned on I guess? with fast clicks col+row keys
so even when I exitt wiht escape
>
Then I go back and try to qwert its traveling within cell. Despite even me seeing regular grid.
I dont know whats going on
